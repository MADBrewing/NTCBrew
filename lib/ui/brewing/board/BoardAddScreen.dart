import 'package:flutter/material.dart';

class BoardAddScreen extends StatelessWidget {
  static const route = "/brewing/board/add";

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
