import 'package:flutter/material.dart';

class StatusCheckScreen extends StatelessWidget {
  static const route = "/brewing/status_check";

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
